server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userdb
    username: root
    password: admin
    application:
    name: ecom-user-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
